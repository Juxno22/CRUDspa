<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zazil Ha Spa</title>
    <link rel="stylesheet" href="/css/agregarP.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Zazil Ha Spa</h1>
            <div class="settings">
                <a href="#">Configuracion</a>
                <a href="#">Ayuda</a>
                <a href="/inicio">Inicio</a>
            </div>
        </header>
        <div class="contenido">
            <div class="agregar">
                <form action="/addP" method="post" class="agregar-form">
                    <p style="font-weight: bold; font-size: 25px; margin: 8px">Agregar Servicio</p>
                    <label for="idProducto">ID</label>
                    <input type="text" name="idProducto" placeholder="id">
                    <label for="nombrePro">Producto</label>
                    <input type="text" name="nombrePro" placeholder="Servicio">
                    <label for="precio">Precio</label>
                    <input type="text" name="precio" placeholder="precio">
                    <label for="descripcion">Descripcion</label>
                    <input type="text" name="descripcion" placeholder="descripcion">
                    <label for="idUsuario">Usuario</label>
                    <input type="text" name="idUsuario" placeholder="Usuario">
                    <label for="idCategoria">Categoria</label>
                    <input type="text" name="idCategoria" placeholder="categoria">
                    <button type="submit" class="agregar-boton">Guardar Servicio</button>
                </form>
            </div>
            <div class="muestra">
                <h2>Productos guardados</h2>
                <table border="1px solid black">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Producto</th>
                            <th>Precio</th>
                            <th>Descripción</th>
                            <th>Usuario</th>
                            <th>Categoría</th>
                            <th>Accion</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Aquí se mostrarán los registros guardados, recorriendo la variables data que los almacena -->
                         <% if (data) { %>
                            <% for (var i=0; i< data.length; i++){ %>
                                <tr>
                                    <td><%= (i +1) %></td>
                                    <td><%= data[i].nombrePro %></td>
                                    <td><%= data[i].precio %></td>
                                    <td><%= data[i].descripcion %></td>
                                    <td><%= data[i].idUsuario %></td>
                                    <td><%= data[i].idCategoria %></td>
                                    <td id="action">
                                        <a href="/deletep/<%= data[i].idProducto %>" style="text-decoration: none;">
                                            <button id="action1">Eliminar</button>
                                        </a>
                                        <a href="/updatep/<%= data[i].idProducto %>" style="text-decoration: none;">
                                            <button id="action2">Editar</button>
                                        </a>
                                    </td>
                                </tr>
                            <% } %>
                         <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <div>
    </div>
</body>
</html>